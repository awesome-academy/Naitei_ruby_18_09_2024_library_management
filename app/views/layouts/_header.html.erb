<header class="fixed top-0 left-0 w-full bg-white shadow-md flex items-center px-10 py-2">
  <%= render "layouts/logo" %>
  <%= render "layouts/search" %>
  <!-- If user's logged in, render user_buttons, otherwise render login_button -->
  <%= render "layouts/user_buttons" %>
</header>
